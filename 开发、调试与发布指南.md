# 开发、调试与发布指南

本文档说明 `dev-toolbox-electron` 在 Windows 上的本地开发、调试和发布流程。项目基于 Electron、Vue 3、TypeScript 和 `electron-vite`，包管理器固定为 pnpm。

## 1. 环境要求

### 必需软件

| 软件 | 要求 | 用途 |
| --- | --- | --- |
| Windows | Windows 10/11 x64 | 当前打包配置仅产出 Windows 安装包与绿色版。 |
| Node.js | 24 或更高版本，建议使用 Node.js 24 LTS | 运行构建脚本；项目使用 `--experimental-strip-types` 执行 `.mts` 脚本。 |
| pnpm | 10.32.1 | 安装和执行项目依赖；版本由 `package.json` 的 `packageManager` 字段锁定。 |
| Git | 当前稳定版 | 获取代码和管理提交。 |
| FFmpeg | Windows x64 的 `ffmpeg.exe` | 视频抽帧、视频抠图，以及生成正式发布包时内置的媒体能力。 |

建议使用 PowerShell 7 或 Windows PowerShell。首次安装依赖和 Electron、Sharp 的预编译二进制时需要能访问 npm 镜像；受代理限制时，先按团队规范配置 pnpm 代理或镜像。

### 安装 Node.js 与 pnpm

安装 Node.js 24 后，在 PowerShell 中启用并固定项目所需的 pnpm：

```powershell
corepack enable
corepack prepare pnpm@10.32.1 --activate
node --version
pnpm --version
```

预期 Node.js 为 `v24.x` 或更高，pnpm 为 `10.32.1`。不要使用系统中旧的 Node.js 版本；例如 Node 14 无法运行本项目的构建工具链。

## 2. 安装依赖

在项目根目录执行：

```powershell
pnpm install --frozen-lockfile
```

日常新增或更新依赖后可执行：

```powershell
pnpm install
```

依赖安装完成后，建议先做一次基础检查：

```powershell
pnpm typecheck
pnpm test
```

`sharp`、Electron 等依赖包含与平台相关的二进制文件，因此应在目标 Windows x64 环境中重新安装依赖。出现下载失败、二进制不匹配或 Node.js 版本变化时，先确认 Node.js 与 pnpm 版本，再在项目根目录重新执行 `pnpm install`。

## 3. 开发与调试启动

### 正常开发启动

```powershell
pnpm dev
```

如果以后 pnpm 装包后再遇到 Error: Electron uninstall，在项目根目录跑一次这个就能恢复
```shell
node node_modules/electron/install.js
```

该命令会启动渲染进程开发服务器，并自动打开 Electron 桌面窗口。保存 Vue、TypeScript 或样式文件后，渲染进程会通过热更新刷新；主进程、预加载脚本及其依赖变动会触发 Electron 重启。

关闭终端中的进程或按 `Ctrl+C` 即可停止开发服务。

### 调试渲染进程

开发窗口获得焦点后按 `Ctrl+Shift+I` 打开 Chromium DevTools。可在其中使用：

- **Console**：查看渲染进程日志和前端报错。
- **Sources**：为 Vue/TypeScript 源码设置断点。
- **Network**：检查前端请求和资源加载。

主进程的 `console.info`、异常和 `electron-vite` 的构建日志会输出在执行 `pnpm dev` 的 PowerShell 窗口中。

### 调试主进程或预加载脚本

需要断点调试 Electron 主进程时，以 Inspector 端口启动：

```powershell
pnpm dev -- --inspect=9229
```

启动后在 Chrome/Edge 打开 `chrome://inspect`，将 `localhost:9229` 加入目标，再点击 Electron 进程的 **inspect**。预加载脚本运行在渲染器上下文中，可在窗口 DevTools 的 **Sources** 面板中调试。

### 验证已构建应用

`pnpm build` 完成后可运行预览模式验证 `out/` 中的构建结果：

```powershell
pnpm build
pnpm start
```

预览模式不等同于最终安装包验证；发布前仍应运行 `pnpm dist` 并安装或运行其产物。

## 4. 构建检查

以下命令不生成安装包，适合提交前或 CI 使用：

```powershell
pnpm test
pnpm typecheck
pnpm build
```

其中 `build` 依次执行渲染进程与 Node 侧类型检查，再由 `electron-vite` 生成 Electron 可执行代码至 `out/`。`out/` 是中间构建目录，不是可直接分发的发行包。

## 5. 发布前准备

### 更新版本号

发行前更新根目录 `package.json` 的 `version`。`electron-builder` 会将该版本写入安装包和绿色版文件名。

### 准备 FFmpeg

正式打包命令会先执行 `pnpm ffmpeg:prepare`。该步骤会验证一个大于 10 MB、可以执行 `-version` 的 `ffmpeg.exe`，然后复制到 `resources/ffmpeg/ffmpeg.exe`，并随发布包一同携带。

脚本默认从以下开发机路径读取 FFmpeg：

```text
D:\code\my\1\dev_toolbox\assets\ffmpeg\ffmpeg.exe
```

在其他开发机上，应在当前 PowerShell 会话显式设置来源路径：

```powershell
$env:DEV_TOOLBOX_FFMPEG_SOURCE = 'C:\tools\ffmpeg\bin\ffmpeg.exe'
pnpm ffmpeg:prepare
```

也可以用一行命令完成设置和打包：

```powershell
$env:DEV_TOOLBOX_FFMPEG_SOURCE = 'C:\tools\ffmpeg\bin\ffmpeg.exe'; pnpm dist
```

该环境变量只对当前 PowerShell 会话生效。即使仓库中已有 `resources/ffmpeg/ffmpeg.exe`，`pack` 和 `dist` 仍会先验证来源文件；来源路径不可用时会失败。

### 可选：更新内置 SD 词典

如需更新随应用发布的 SD 中文词典，执行：

```powershell
pnpm dictionary:build
```

该命令会从 GitHub 下载数据并更新 `resources/data/sd_tags.json`，因此需要网络连接。未计划更新词典时，无需执行此命令。

## 6. 打包与发布

### 生成未压缩应用目录

```powershell
pnpm pack
```

`pack` 会准备 FFmpeg、执行类型检查和 `electron-vite build`，然后由 `electron-builder --dir` 生成未压缩的应用目录。它适合在正式出包前快速验证应用的目录结构和运行情况，不生成可交付的安装版或绿色版文件。

### 生成正式发行包

```powershell
pnpm dist
```

`dist` 的完整流程为：准备并校验 FFmpeg，执行类型检查，构建到 `out/`，再由 `electron-builder` 打包。产物写入 `dist/`：

| 产物 | 文件名规则 | 使用方式 |
| --- | --- | --- |
| NSIS 安装版 | `开发者工具箱-<version>-安装版.exe` | 面向需要安装、可选择安装目录的用户。 |
| Portable 绿色版 | `开发者工具箱-<version>-绿色版.exe` | 单文件直接运行，适合免安装分发。 |

当前打包配置见 `electron-builder.yml`：应用标识为 `com.devtoolbox.app`，图标为 `resources/icon.ico`，并会把 `resources/data` 和 `resources/ffmpeg/ffmpeg.exe` 复制到应用资源目录。

### 发布验收清单

- 确认 `package.json` 的版本号正确。
- 确认 `pnpm test`、`pnpm typecheck`、`pnpm build` 均通过。
- 确认 `pnpm dist` 成功，且 `dist/` 中同时生成安装版和绿色版。
- 在干净的 Windows 环境安装 NSIS 安装版，并启动验证基础功能。
- 直接运行绿色版，验证其无需依赖开发目录即可启动。
- 验证依赖 FFmpeg 的视频功能能够识别并调用内置 FFmpeg。

## 7. 常见问题

### `node --experimental-strip-types` 无法识别或脚本无法运行

通常是 Node.js 版本过低。切换到 Node.js 24 或更高版本后，重新执行 `pnpm install`。

### `pnpm ffmpeg:prepare` 报找不到文件或 FFmpeg 无法运行

确认 `DEV_TOOLBOX_FFMPEG_SOURCE` 指向实际存在的 Windows x64 `ffmpeg.exe`。可先单独执行：

```powershell
& 'C:\tools\ffmpeg\bin\ffmpeg.exe' -version
```

命令必须成功，并且文件大小需大于 10 MB。

### `pnpm dist` 成功但视频功能不可用

检查打包后的应用资源中是否有 `resources/ffmpeg/ffmpeg.exe`，并在目标机器上检查安全软件或系统策略是否阻止其执行。重新执行 `pnpm ffmpeg:prepare` 后再打包可排除资源过期或损坏问题。

### 安装依赖时 Electron 或 Sharp 下载失败

先确认网络、代理和 npm 镜像配置可用，再删除本次失败产生的依赖目录并重新执行 `pnpm install`。不要跨 Node.js 主版本复用旧的 `node_modules`。
